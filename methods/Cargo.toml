[package]
name = "policy-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = { version = "3.0" }
# risc0-build = { path = "../../../risc0/build" }

[package.metadata.risc0]
methods = ["guest"]
